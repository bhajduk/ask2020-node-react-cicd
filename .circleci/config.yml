 version: 2.1
 orbs:
   node: circleci/node@2.1.0
 jobs:
   build-and-test:
    executor: node/default
    steps:
     - checkout
     - node/install-packages
     - run:
         working_directory: ~/Pulpit/ask2020-node-react-cicd/client/
         command: npm test
 workflows:
   build-and-test:
    jobs:
     - build-and-test
