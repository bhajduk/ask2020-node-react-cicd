 version: 2.1
 orbs:
   node: circleci/node@3.0.0
   docker: circleci/docker@1.2.1
 jobs:
   build-and-test:
    executor: node/default
    steps:
     - checkout
     - run: cd client && npm install
     - run: cd client && npm test
     - run: cd server && npm install 
   deploy:
    executor: docker/docker
    steps:
     - checkout
     - run: cd client && docker build . -t bhajduk/lab2:client
     - run: cd client && docker push bhajduk/lab2:client
     - run: cd server && docker build . -t bhajduk/lab2:server
     - run: cd server && docker push bhajduk/lab2:server
 workflows:
   build-and-test-deploy:
    jobs:
     - build-and-test:
       filters:
         branches:
           only:
             - master
     - deploy
